@model ADS.LAPEM.Web.Areas.Catalogo.Models.ColorViewModel

@using (Html.BeginForm("Delete", "Color", FormMethod.Post, new { id = "frmColor" }))
{ 
    <p>
        Se eliminará del sistema el color "@Model.Color.Nombre".
        ¿Desea Continuar?
    </p>
    
    <p>@Html.HiddenFor(x => x.Color.Id)</p>
}