@model ADS.LAPEM.Web.Areas.Catalogo.Models.TipoPruebaViewModel

@{
    ViewBag.Title = "_Form";
}

@using (Html.BeginForm("Create", "TipoPrueba", FormMethod.Post, new { id = "frmTipoPrueba" }))
{ 
    <p>@Html.ValidationSummary()</p>
    
    <div>
        <ul>
            @foreach (string mensaje in Model.Mensajes)
            { 
                <li style="color: red">@mensaje</li>
            }
        </ul>
    </div>
    
    if (Model.TipoPrueba.Id > 0)
    { 
        <p>@Html.HiddenFor(x => x.TipoPrueba.Id)</p>
    }
    
    <div>
        <fieldset>
            <legend>ABC Tipo Prueba</legend>

            <div class="editor-label">
                 @Html.Label("*Nombre: ")
            </div>
            <div class="editor-field">
                @Html.ValidationMessageFor(x => x.TipoPrueba.Nombre, "*")
                @Html.TextBoxFor(x => x.TipoPrueba.Nombre, new { @class = "input150" })
            </div>

            <div class="editor-label">
                 @Html.Label("Descripcion: ")
            </div>
            <div class="editor-field">
                @Html.ValidationMessageFor(x => x.TipoPrueba.Descripcion, "*")
                @Html.TextAreaFor(x => x.TipoPrueba.Descripcion, new { @class = "inputTextArea" })
            </div>
        </fieldset>
        
    </div>
}